import { theme } from '@styles/theme';
import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
  container: {},
  box: {
    backgroundColor: theme.colors.gray.gray06,
    overflow: 'hidden',
  },
});

export default styles;
