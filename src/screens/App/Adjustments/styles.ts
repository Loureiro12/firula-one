import { theme } from "@styles/theme";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  contentProfileEditActions: {
    gap: theme.spacing[4],
    width: "100%",
  }
})