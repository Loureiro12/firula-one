import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  formContainer: {
    gap: 16,
    paddingHorizontal: 16,
    paddingBottom: 20,
  },
  
  submitButton: {
    marginTop: 24,
  },
});